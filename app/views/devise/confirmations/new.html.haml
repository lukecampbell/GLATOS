%h2 Resend confirmation instructions
= semantic_form_for(resource, :as => resource_name, :url => confirmation_path(resource_name), :html => { :method => :post }) do |f|
  =f.inputs do
    = f.input :email
  = f.actions do
    = f.action :submit, :as => :button, :label => "Resend confirmation instructions", :button_html => { :id => "Resend confirmation instructions"}

%br

= render :partial => "devise/shared/links"
